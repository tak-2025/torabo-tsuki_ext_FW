# The encoder's push button on the PERIPHERAL half, as a plain GPIO key that is
# relayed to the central over the split input channel. No store here — the
# peripheral only forwards the event; the central decides what it does.
CONFIG_INPUT=y
CONFIG_INPUT_GPIO_KEYS=y

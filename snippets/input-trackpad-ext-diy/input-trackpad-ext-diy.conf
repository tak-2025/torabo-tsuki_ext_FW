# DIY extender trackpad overlay — applied on top of `input-trackpad-ext`, which
# already enables CONFIG_IQS7211E (+ scroller inertia). Nothing extra needed
# here yet; add DIY-only Kconfig differences (e.g. inertia/edge tuning) below.

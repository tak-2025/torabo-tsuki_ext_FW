# The extender LED on the PERIPHERAL half. It holds no rules: the central pushes a
# rendered (colour, pattern) over the split. The only thing it decides for itself
# is what to show while the link is DOWN — precisely the state the central cannot
# reach it in.
CONFIG_ZMK_LED_CONFIG=y

/*
 * Host-test stub for <zephyr/types.h>.
 *
 * PHASE 0 RULE: nothing under test/ may require a change to production code.
 * These stubs exist so the pure wire codecs (config_state.c and friends) can be
 * compiled and exercised by a native gcc/clang, byte for byte the same source
 * the firmware links.
 */
#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
